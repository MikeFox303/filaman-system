name: Stage X2D plugin 1.3.9 packaging

on:
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize]

permissions:
  contents: write

jobs:
  package:
    if: github.head_ref == 'release/x2d-plugin-1.3.9'
    permissions:
      contents: write
    uses: MikeFox303/filaman-system/.github/workflows/one-shot-x2d-plugin-1.3.9.yml@release/x2d-plugin-1.3.9
